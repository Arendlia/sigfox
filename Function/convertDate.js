/**
 * Function for convert timetostamp to datetime
 * 
 * 
 */

export function convertTimestampToDatetime(timestamp) {
    let timestamp = timestamp;
    var date = new Date(timestamp);  
    return console.log(date);
}